[package]
name = "jprod"
version = "0.1.0"
authors = ["Jonathan"]

build = "build.rs"

[dependencies]
rlibc = "1.0"

[build-dependencies]
walkdir = "1"

[profile.release]
opt-level = "s"
panic = "abort"

[profile.dev]
panic = "abort"