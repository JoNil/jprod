#version 450 core

in vec2 frag_uv;
in vec3 frag_pos;

layout(location = 0) out vec3 color;
layout(location = 1) out vec3 pos;

void main()
{
    color = vec3(frag_uv, 1.0);
    pos = frag_pos;
}